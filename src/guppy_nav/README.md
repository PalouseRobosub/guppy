# guppy_nav

This package houses code for the navigation and movement of Guppy across several waypoints. Primarily, the waypoints are chosen as subsets of the path and then are translated to set holding poses with `/reset_holding_pose` from the `guppy_control` package.

This package will also contain code for the BehaviorTree.CPP libraries and utilize them for structuring the autonomy in XML files.
